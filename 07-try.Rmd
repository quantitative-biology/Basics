# Just to try
Some _significant_ applications are demonstrated in this chapter.

## Example one

See Figure \@ref(fig:cars-plot).

```{r cars-plot, fig.cap="The cars data.", fig.alt = "A scatter plot.", echo=FALSE}
par(mar = c(4, 4, .2, .1))
plot(cars)  # a scatterplot
```
